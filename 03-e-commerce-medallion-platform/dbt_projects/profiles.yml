name: 'medallion_platform'
version: '1.0.0'
config-version: 2

profile: 'medallion_platform'

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  medallion_platform:
    silver:
      +materialized: table